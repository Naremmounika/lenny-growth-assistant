# Agent Transcripts

This directory contains sanitized development transcripts and logs related
to the Claude Agent SDK integration.

Secrets such as API keys, database credentials, tokens, and private
configuration values must never be stored here.

The logs document both successful implementation steps and failed attempts
or corrections so that the development process is reproducible.